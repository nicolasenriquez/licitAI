{
  "name": "twenty-design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical DTCG design token source and deterministic adapters.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "src",
    "generated"
  ],
  "scripts": {
    "build": "tsx src/compiler/cli.ts build",
    "generate": "tsx src/compiler/cli.ts generate",
    "validate": "tsx src/compiler/cli.ts validate",
    "test": "jest --config=jest.config.mjs"
  }
}
