# Note that provide always without trailing forward slash to have expected behaviour
FRONTEND_BASE_URL=http://localhost:3001
BACKEND_BASE_URL=http://localhost:3000
DEFAULT_LOGIN=tim@apple.dev
DEFAULT_PASSWORD=tim@apple.dev
ANALYST_LOGIN=
WEBSITE_URL=https://twenty.com

# Mercado Publico V2 baseline (issue 18): must match the frontend build flag.
# The smoke spec reads it from process.env; provision-baseline.mjs writes it here.
# Set ANALYST_LOGIN in the ignored .env to a seeded user without sync-operator assignment.
