# Harness Compatibility

The canonical OpenSpec policy lives in this skill. Native command entrypoints,
if added, must be thin adapters that delegate to `.agents/skills/`.

Do not copy the OpenSpec workflow into any native command directory.
