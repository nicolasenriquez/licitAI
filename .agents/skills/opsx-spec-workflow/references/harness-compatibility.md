# Harness Compatibility

The canonical OpenSpec policy lives in this skill. Native command entrypoints
are adapters only; read the adapter for the active harness when command syntax
or workflow compatibility needs verification.

- Codex adapters live under `.codex/commands/`.
- OpenCode adapters live under `.opencode/commands/`.

Do not copy the OpenSpec workflow into either native command directory.
