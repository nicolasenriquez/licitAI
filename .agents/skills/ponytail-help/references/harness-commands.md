# Harness Command Forms

Use the native command form for the active harness:

- Codex: `@ponytail`, `@ponytail-review`, and `@ponytail-help`.
- OpenCode: `/ponytail`, `/ponytail-review`, and `/ponytail-help`.
- Claude Code: the slash-command forms above.
