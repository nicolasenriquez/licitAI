# Harness Dispatch Adapter

Use this reference when the design workflow needs parallel alternatives.

Preserve the requested role, parallelism, file scope, and result format while
translating the dispatch to the active harness. Codex uses its configured
parallel-agent dispatch; OpenCode uses parallel `@agent` mentions in one
response. Collect every alternative before presenting or comparing designs.
