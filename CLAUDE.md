# CLAUDE.md

Please read `AGENTS.md` in this repository for the full working contract.

For the repo-workspace routing rollout:

- Read `CONTEXT-MAP.md` at the repository root before choosing a folder-local context.
- If the selected surface is `openspec/`, also read `openspec/AGENTS.md` and `openspec/CONTEXT.md`.
- If the selected surface is `docs/`, also read `docs/AGENTS.md` and `docs/CONTEXT.md`.
- If the selected surface is `packages/`, also read `packages/AGENTS.md` and `packages/CONTEXT.md`.
- If the selected surface is `.codex/`, also read `.codex/AGENTS.md` and `.codex/CONTEXT.md`.
- If the current folder does not match the task, return to the root map and reroute instead of improvising.
